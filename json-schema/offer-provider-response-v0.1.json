{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agentoffernetwork.org/schema/offer-provider-response/v0.1",
  "title": "AgentOffer OfferProvider Response Schema v0.1",
  "description": "Response body returned by a Partner's OfferProvider endpoint. Two shapes are permitted: (a) success envelope { request_id, offers } and (b) error envelope following the AON ApiResponse contract { code, message, data, extra }. Uses RFC 2119 requirement levels.",
  "oneOf": [
    { "$ref": "#/$defs/SuccessEnvelope" },
    { "$ref": "#/$defs/ErrorEnvelope" }
  ],
  "$defs": {
    "SuccessEnvelope": {
      "type": "object",
      "description": "Success response envelope. Carried over HTTP 200. offers[] elements SHOULD conform to https://agentoffernetwork.org/schema/offer/v0.1 (offer-schema-v0.1.json).",
      "required": ["request_id", "offers"],
      "properties": {
        "request_id": {
          "type": "string",
          "description": "Echoes the `request_id` from the incoming OfferProvider request so the full agent → AON → Partner chain shares one correlation id (UUIDv7). REQUIRED in Partner success responses: Partners MUST echo the value AON sent and MUST NOT replace it with a Partner-generated correlation id."
        },
        "offers": {
          "type": "array",
          "description": "List of offer objects conforming to offer-schema-v0.1.json. MAY be an empty array when no matches are found.",
          "items": {
            "type": "object"
          }
        }
      },
      "additionalProperties": false
    },
    "ErrorEnvelope": {
      "type": "object",
      "description": "Error response envelope aligned with the AON ApiResponse contract. Carried over HTTP 4xx/5xx.",
      "required": ["code", "message", "data", "extra"],
      "properties": {
        "code": {
          "type": "string",
          "enum": [
            "BAD_REQUEST",
            "UNAUTHORIZED",
            "FORBIDDEN",
            "RATE_LIMITED",
            "INTERNAL_ERROR"
          ],
          "description": "Business-level error code. MUST be one of the enumerated values; additional codes MAY be introduced in future revisions."
        },
        "message": {
          "type": "string",
          "description": "Human-readable description of the error. MAY be empty when no additional context is available."
        },
        "data": {
          "type": "object",
          "description": "Always an empty object for error responses per the AON ApiResponse contract."
        },
        "extra": {
          "type": "object",
          "description": "Optional diagnostic/extension fields (e.g., retry_after_seconds, debug metadata). Empty object when unused."
        }
      },
      "additionalProperties": false
    }
  }
}
